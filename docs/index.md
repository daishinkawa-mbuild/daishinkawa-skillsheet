# Daishinkawa Skillsheet

このディレクトリの Markdown を Nextra で配信するための初期セットアップです。

## 使い方

- `docs/` 配下に Markdown ファイルを追加します
- 開発時は `pnpm docs:dev` を実行します
- ビルド確認は `pnpm docs:build` を実行します

`apps/docs` 側は配信用のアプリなので、コンテンツは `docs/` を正本として更新してください。
